#include <stdio.h>

int main() {
    int n,p,temp;
    scanf("%d",&n);
    int arr[n+1];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
        
    }
    
    //printf("The value you want to add and position");
    scanf("%d",&p);

    for(int i=p-1;i<n-1;i++)
    {
        
        arr[i]=arr[i+1];
        
        
    }
     for(int i=0;i<n-1;i++)
    {
        printf("%d ",arr[i]);
        
    }
    
    
}
